# Chain Configuration
CHAIN_TYPE=bnb-testnet

# Storage Node Configuration
EXPOSE_PORT=8082

# Railway will automatically set RAILWAY_PUBLIC_DOMAIN
# This will be used to construct EXPOSE_URL
EXPOSE_URL=https://${RAILWAY_PUBLIC_DOMAIN}

# Optional: If you want to use a specific storage directory
# STORAGE_PATH=/root/.store

# Optional: Add your secret key if needed for authenticated operations
# SK=your_secret_key_here